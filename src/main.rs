#![allow(non_snake_case)]
fn main() {
    println!("Hello, world!");
    // input some number
    let mut input = String::new();
    // input
    std::io::stdin().read_line(&mut input).unwrap();
    
}
